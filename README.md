# first-typescript-running-node
A repo I will use to get my Typescript NodeJS Server setup correctly

# Code run
sudo npm install --global --force yarn
yarn init -y
mkdir src
tsc --init
yarn add express ts-node
yarn add -D nodemon @types/express typescript
